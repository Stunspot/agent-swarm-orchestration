- **Topology:** Adopt `Assemble` regime with exactly three concurrent read-only workers targeting `billing`, `search`, and `notifications`; root retains mission custody while agents inspect their respective folders for retry handling observations and unknowns without writing to shared state.  
- **Dispatch Boundaries:** Each worker receives a packet containing only the named folder path, read-only authority over that surface, an objective to cite observed retry behaviors or missing evidence, and a return contract requiring cited sources plus explicit unknowns; no generic tests, schemas, paths, endpoints, sample data, or hypothetical findings are drafted.  
- **Reconciliation Plan:** Root waits for all three returns as `agent-reported`, then merges them into a comparison that isolates the riskiest supported inconsistency (e.g., divergent retry logic across services) while preserving unresolved evidence conflict and avoiding premature synthesis of unverified claims.  
- **Next Move:** Upon receipt, root verifies cited sources against authoritative state; if any return is missing or unverifiable, mark that slice `PARTIAL — DEPENDENCY UNAVAILABLE` and hold downstream integration pending the exact identification method for the absent source rather than substituting generic content.
